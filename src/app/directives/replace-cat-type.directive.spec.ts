import { ReplaceCatTypeDirective } from './replace-cat-type.directive';

describe('ReplaceCatTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceCatTypeDirective();
    expect(directive).toBeTruthy();
  });
});
